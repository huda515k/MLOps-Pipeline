# Optimized requirements - grouped for faster builds
# Core ML (install first - most stable)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
pyarrow>=10.0.0

# MLflow and Experiment Tracking
mlflow>=2.9.0
dagshub>=0.3.19

# Data Version Control
dvc>=3.38.0
dvc-s3>=3.1.0

# API and Web Framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
requests>=2.31.0

# Data Quality (lighter alternatives)
pandas-profiling>=3.6.0

# Monitoring
prometheus-client>=0.19.0

# Airflow (install last - heaviest)
apache-airflow==2.7.3
apache-airflow-providers-docker>=3.7.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0

